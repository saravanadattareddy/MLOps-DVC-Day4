1. Create git repo and clone it in local.
2. Create mycode.py and add code to it.
3. Do a git commit before dvc install.
#pip installdvc
4. now we do "dvc init" (creates .dvcignore, .dvc)
5. Now do "mkdir s3"(create new 23 directory)
6. Now we do "dvc remote add -d myremote s3"
7. Next "dvc add data/"
